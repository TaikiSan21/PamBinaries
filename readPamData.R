# reading pam data
# the @function in MATLAB is because MATLAB isnt functional,
# so it can't just pass around functions. In R we can just pass
# the function directly, so 
# fileInfo$readModuleData <- readAISData (this is a function)
#
# Then later this gets called fileInfo$readModuleData(inputs)